{
  "widget": "set-page",
  "if": "uci.loadConfig('network') && ( network = uci.configs.network )",
  "content": [
    {
      "widget": "set-section",
      "events": {
        "onInit": "iface_dump = ubus('network.interface', 'dump', `$.interface[?(@.device!='lo')]`)"
      },
      "options": { "title": "Interfaces" }
    }
  ]
}
