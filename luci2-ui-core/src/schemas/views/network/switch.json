{
  "widget": "tabs",

  "options": {
    "tabLabels": ["Switch", "Vlan", "Ports"]
  },

  "content": [
    {
      "widget": "schema-form",
      "options": {
        "schema=": "uci.getSchema('network','@switch')",
        "bind": "uci.configs.network['@switch']"
      }
    },
    {
      "widget": "schema-form",
      "options": {
        "schema=": "uci.getSchema('network','@switch_vlan')",
        "bind": "uci.configs.network['@switch_vlan']"
      }
    },
    {
      "widget": "schema-form",
      "options": {
        "schema=": "uci.getSchema('network','@switch_port')",
        "bind": "uci.configs.network['@switch_port']"
      }
    }
  ]
}
