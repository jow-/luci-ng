{
  "widget": "container",
  "if": "uciLoad('network')",
  "content": [
    "uciUI('network','@interface', parseInt($routeParam.id), {level: 0, widgets: {objectLevel: ['set-subpage','set-section','set-expansion']}})"
  ]
}
